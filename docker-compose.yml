version: '3.8'

services:
  ui:
    build: .
    container_name: kurrent-ocr-ui
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://localhost:8000/v1/transcribe
    restart: always

